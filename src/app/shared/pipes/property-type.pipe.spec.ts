import { PropertyTypePipe } from './property-type.pipe';

describe('PropertyTypePipe', () => {
  it('create an instance', () => {
    const pipe = new PropertyTypePipe();
    expect(pipe).toBeTruthy();
  });
});
